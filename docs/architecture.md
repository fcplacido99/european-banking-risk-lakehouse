# Architecture

The v1 boundary is local verified acquisition, manual Databricks Volume upload,
and a five-task sequential Databricks Workflow producing Bronze, Silver, and
Gold Delta tables.

An implementation-accurate Mermaid diagram will replace this placeholder near
the release-candidate stage.
