# Resources

This directory is reserved for small, public, repository-safe assets such as
sample outputs or diagrams. Raw EBA datasets, generated manifests containing
local paths, and large generated outputs are excluded.
