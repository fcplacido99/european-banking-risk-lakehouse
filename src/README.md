# Python source

The importable `eba_lakehouse` package will be created in Week 2.

Reusable acquisition, parsing, validation, and contract logic belongs in the
package. Databricks notebooks should call package functions rather than contain
duplicate business logic.
