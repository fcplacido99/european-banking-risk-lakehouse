# Configuration

Versioned, non-secret source and pipeline configuration belongs here.

`sources.yml` will be introduced with deterministic acquisition in Week 3.
Do not store credentials, personal workspace identifiers, or raw data here.
